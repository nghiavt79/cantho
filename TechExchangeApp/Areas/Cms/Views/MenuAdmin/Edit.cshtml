@model TechExchangeApp.Areas.Cms.Controllers.MenuFormVm
@{
    ViewData["Title"] = "Sửa Menu";
}

<form asp-action="Edit" method="post" id="menuForm">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="MenuId" />
    <div class="card card-outline card-primary shadow-sm">
        <div class="card-header d-flex align-items-center flex-wrap gap-2">
            <h3 class="card-title mb-0">
                <i class="fas fa-pen me-1"></i> Sửa Menu #@Model.MenuId
            </h3>
            <div class="ms-auto d-flex gap-2">
                <button type="submit" class="btn btn-primary btn-sm">
                    <i class="fas fa-save me-1"></i> Cập nhật
                </button>
                <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
                    <i class="fas fa-arrow-left me-1"></i> Quay lại
                </a>
            </div>
        </div>
        <div class="card-body">
            @if (Model.Created.HasValue)
            {
                <div class="alert alert-light small mb-3">
                    <i class="fas fa-info-circle me-1"></i>
                    Tạo bởi <strong>@(Model.Creator ?? "—")</strong> lúc <strong>@Model.Created?.ToString("dd/MM/yyyy HH:mm")</strong>
                </div>
            }
            @await Html.PartialAsync("_MenuForm", Model)
        </div>
    </div>
</form>

@section Styles {
<link rel="stylesheet" href="https://cdn.ckeditor.com/ckeditor5/43.0.0/ckeditor5.css" />
<style>.ck-editor__editable { min-height: 300px; }</style>
}

@section Scripts {
<script src="https://cdn.ckeditor.com/ckeditor5/43.0.0/ckeditor5.umd.js"></script>
<script>
$(function(){
    $('#selParent').select2({ theme: 'bootstrap-5', placeholder: '-- Gốc --', allowClear: true, width: '100%' });

    const { ClassicEditor, Essentials, Bold, Italic, Link, Paragraph,
            Heading, List, BlockQuote, Table, TableToolbar,
            SourceEditing, Alignment, Font, HtmlEmbed } = CKEDITOR;

    ClassicEditor.create(document.querySelector('#editorDescription'), {
        plugins: [
            Essentials, Bold, Italic, Link, Paragraph, Heading,
            List, BlockQuote, Table, TableToolbar,
            SourceEditing, Alignment, Font, HtmlEmbed
        ],
        toolbar: {
            items: [
                'heading', '|',
                'bold', 'italic', 'link', '|',
                'alignment', '|',
                'fontSize', 'fontColor', '|',
                'bulletedList', 'numberedList', '|',
                'blockQuote', 'insertTable', 'htmlEmbed', '|',
                'sourceEditing', '|',
                'undo', 'redo'
            ]
        },
        table: {
            contentToolbar: ['tableColumn', 'tableRow', 'mergeTableCells']
        }
    }).then(editor => {
        window._descEditor = editor;
        document.getElementById('menuForm').addEventListener('submit', function() {
            document.getElementById('editorDescription').value = editor.getData();
        });
    }).catch(err => console.error('CKEditor init error:', err));
});
</script>
}
