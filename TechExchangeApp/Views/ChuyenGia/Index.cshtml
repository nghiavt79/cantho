@using TechExchangeApp.ViewModel
@model ChuyenGiaIndexVm

@{
    ViewData["Title"] = Model.PageTitle;
}

@section Styles {
    <link rel="stylesheet" href="~/css/chuyen-gia.css" asp-append-version="true" />
}

<div class="expert-page">

    @* =============== PAGE HEADING =============== *@
    <section class="expert-hero">
        <div class="expert-container">
            <h1 class="expert-hero__title">@Model.PageTitle</h1>
            <p class="expert-hero__subtitle">
                Kết nối với đội ngũ chuyên gia trong chuyển giao công nghệ, sở hữu trí tuệ, đầu tư và đổi mới sáng tạo.
            </p>
            <span class="expert-hero__accent"></span>
        </div>
    </section>

    <div class="expert-container">
        <div class="expert-layout">

            @* =============== LEFT: CATEGORY SIDEBAR =============== *@
            <aside class="expert-sidebar">
                <p class="expert-sidebar__title">Lĩnh vực tư vấn</p>
                <ul class="expert-sidebar__list">
                    <li class="expert-sidebar__item @(Model.CateId == 0 ? "is-active" : "")">
                        <a href="/chuyen-gia">Tất cả</a>
                    </li>
                    @foreach (var c in Model.Categories)
                    {
                        <li class="expert-sidebar__item @(Model.CateId == c.Id ? "is-active" : "")">
                            <a href="/chuyen-gia?cateId=@c.Id">@c.Title</a>
                        </li>
                    }
                </ul>
            </aside>

            @* =============== RIGHT: EXPERT LIST =============== *@
            <section class="expert-content">

                <div class="expert-content__header">
                    <h2 class="expert-content__title">Đội ngũ chuyên gia</h2>
                    @if (Model.TotalCount > 0)
                    {
                        <p class="expert-content__subtitle">@Model.TotalCount chuyên gia đang tham gia tư vấn</p>
                    }
                </div>

                @if (!Model.Items.Any())
                {
                    <div class="expert-empty">Không có chuyên gia nào.</div>
                }
                else
                {
                    <div class="expert-grid">
                        @foreach (var item in Model.Items)
                        {
                            <div class="expert-card">
                                <div class="expert-card__media">
                                    <a href="@item.DetailUrl">
                                        <img src="@item.ImageUrl" alt="@item.FullName" />
                                    </a>
                                </div>
                                <div class="expert-card__body">
                                    <h3 class="expert-card__name">
                                        <a href="@item.DetailUrl">@item.FullName</a>
                                    </h3>
                                    @if (!string.IsNullOrEmpty(item.ChucVu))
                                    {
                                        <div class="expert-card__title">@item.ChucVu</div>
                                    }
                                    @if (!string.IsNullOrEmpty(item.CoQuan))
                                    {
                                        <div class="expert-card__organization">@item.CoQuan</div>
                                    }
                                    @if (item.Rating > 0)
                                    {
                                        <div class="expert-card__rating">
                                            @for (int i = 1; i <= 5; i++)
                                            {
                                                <span class="expert-card__star @(i <= item.Rating ? "is-filled" : "")">★</span>
                                            }
                                        </div>
                                    }
                                    <div class="expert-card__actions">
                                        <a href="@item.DetailUrl" class="expert-card__btn">Xem hồ sơ</a>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>

                    @* ── PAGINATION (CMS style) ─────────────────────────── *@
                    @if (Model.TotalPage > 1)
                    {
                        <div class="expert-pagination">
                            <div class="expert-pagination__info">
                                Trang <strong>@Model.CurrentPage</strong> / <strong>@Model.TotalPage</strong>
                                &nbsp;·&nbsp; Tổng <strong>@Model.TotalCount</strong> chuyên gia
                            </div>
                            <ul class="pagination pagination-sm mb-0 expert-pagination__list">
                                <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                                    <a class="page-link" href="/chuyen-gia?cateId=@Model.CateId&page=1">
                                        <i class="fas fa-angles-left"></i>
                                    </a>
                                </li>
                                <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                                    <a class="page-link" href="/chuyen-gia?cateId=@Model.CateId&page=@(Model.CurrentPage - 1)">
                                        <i class="fas fa-angle-left"></i>
                                    </a>
                                </li>
                                @foreach (var p in Model.Pages)
                                {
                                    <li class="page-item @(p == Model.CurrentPage ? "active" : "")">
                                        <a class="page-link" href="/chuyen-gia?cateId=@Model.CateId&page=@p">@p</a>
                                    </li>
                                }
                                <li class="page-item @(Model.CurrentPage == Model.TotalPage ? "disabled" : "")">
                                    <a class="page-link" href="/chuyen-gia?cateId=@Model.CateId&page=@(Model.CurrentPage + 1)">
                                        <i class="fas fa-angle-right"></i>
                                    </a>
                                </li>
                                <li class="page-item @(Model.CurrentPage == Model.TotalPage ? "disabled" : "")">
                                    <a class="page-link" href="/chuyen-gia?cateId=@Model.CateId&page=@Model.TotalPage">
                                        <i class="fas fa-angles-right"></i>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    }
                }

            </section>
        </div>
    </div>
</div>
